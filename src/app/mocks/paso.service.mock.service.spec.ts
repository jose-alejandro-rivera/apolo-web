import { TestBed } from '@angular/core/testing';

import { PasoMockService } from './paso.service.mock.service';

describe('Paso.Service.MockService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: Paso.Service.MockService = TestBed.get(Paso.Service.MockService);
  //   expect(service).toBeTruthy();
  // });
});
